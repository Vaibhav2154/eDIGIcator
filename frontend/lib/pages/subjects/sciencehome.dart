import 'package:flutter/material.dart';

class ScienceHome extends StatelessWidget {
  const ScienceHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Science Syllabus'),
      ),
      body: const Center(
        child: Text(''), // Empty body for now
      ),
    );
  }
}
