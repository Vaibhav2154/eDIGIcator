import 'package:flutter/material.dart';

class SocialHome extends StatelessWidget {
  const SocialHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Social Syllabus'),
      ),
      body: const Center(
        child: Text(''), // Empty body for now
      ),
    );
  }
}
